# 환경변수 템플릿 파일
# 이 파일을 복사하여 .env.local 파일을 만들고 실제 값을 입력하세요

# =============================================================================
# 필수 환경변수 (반드시 설정해야 함)
# =============================================================================

# API 서버 기본 URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# 애플리케이션 이름
NEXT_PUBLIC_APP_NAME=Acorn

# =============================================================================
# 선택적 환경변수
# =============================================================================

# 애플리케이션 URL (배포 시)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 애플리케이션 버전
NEXT_PUBLIC_APP_VERSION=1.0.0

# 인증 관련 (NextAuth.js 사용 시)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-key-change-this-in-production

# 외부 서비스 API 키들 (선택적)
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
NEXT_PUBLIC_SENTRY_DSN=

# 로그 레벨 (development, production에서 다르게 설정)
LOG_LEVEL=debug

# 분석 도구 활성화 여부
ENABLE_ANALYTICS=false

# =============================================================================
# 개발 환경 전용 (프로덕션에서는 다른 값 사용)
# =============================================================================

# 데이터베이스 URL (개발용)
# DATABASE_URL=postgresql://username:password@localhost:5432/acorn_dev

# Redis URL (개발용)
# REDIS_URL=redis://localhost:6379
